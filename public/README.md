Place static assets here. Vite serves files from the `public/` directory at the project root as-is.

Examples:
- `public/favicon.svg` -> available at `/favicon.svg`
- `public/sample.txt` -> available at `/sample.txt`
